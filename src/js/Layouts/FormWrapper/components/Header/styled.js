import styled from 'styled-components';

import { rem } from 'polished';

export const HeadingTitle = styled.h2`
  font-size: ${rem(19)};
  font-weight: 400;
  letter-spacing: ${rem(1)};
  color: #666;
  border-bottom: ${rem(1)} solid #70707042;
  padding: ${rem(24)} ${rem(44)} ${rem(30)};
`;
